image:
  repository: 10.43.238.235:5000/front
  tag: "01"
  pullPolicy: IfNotPresent

imagePullSecrets: "my-registry-secret"

service:
  port: 443

ingress:
  enabled: true
  className: "nginx"
  annotations:
    kubernetes.io/ingress.class: "nginx"
  path: "/"
  host: "api.domain.com"
  pathType: Prefix
